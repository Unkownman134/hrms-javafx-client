<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox spacing="20.0" alignment="TOP_CENTER" xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.gd.hrmsjavafxclient.controller.employee.ChangePasswordController"
      stylesheets="@../../style/hrms-styles.css" styleClass="root">
    <padding><Insets top="50" right="50" bottom="50" left="50"/></padding>

    <VBox spacing="10" alignment="CENTER" maxWidth="400">
        <Label text="修改登录密码" style="-fx-font-size: 24px; -fx-font-weight: bold;"/>
        <Label text="请保护好您的账号安全" style="-fx-text-fill: -fx-text-muted;"/>
    </VBox>

    <GridPane vgap="20" hgap="15" alignment="CENTER" styleClass="login-card" maxWidth="450">
        <padding><Insets top="30" right="30" bottom="30" left="30"/></padding>

        <Label text="新 密 码:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
        <StackPane styleClass="password-wrapper" GridPane.rowIndex="1" GridPane.columnIndex="1" HBox.hgrow="ALWAYS">
            <TextField fx:id="newTextField" visible="false" managed="false" promptText="请输入新密码"/>
            <PasswordField fx:id="newPasswordField" promptText="请输入新密码"/>
            <ToggleButton fx:id="eyeButton1" onAction="#toggleNewPassword" styleClass="eye-button"
                          StackPane.alignment="CENTER_RIGHT" text="👁" opacity="0"/>
        </StackPane>

        <Label text="确认新密码:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
        <StackPane styleClass="password-wrapper" GridPane.rowIndex="2" GridPane.columnIndex="1" HBox.hgrow="ALWAYS">
            <TextField fx:id="confirmTextField" visible="false" managed="false" promptText="请再次输入新密码"/>
            <PasswordField fx:id="confirmPasswordField" promptText="请再次输入新密码"/>
            <ToggleButton fx:id="eyeButton2" onAction="#toggleConfirmPassword" styleClass="eye-button"
                          StackPane.alignment="CENTER_RIGHT" text="👁" opacity="0"/>
        </StackPane>

        <HBox spacing="20" alignment="CENTER_RIGHT" GridPane.rowIndex="3" GridPane.columnIndex="1">
            <Button text="重置" onAction="#handleReset" styleClass="button-secondary"/>
            <Button fx:id="submitButton" text="确认修改" onAction="#handleSubmit" styleClass="button-primary"/>
        </HBox>
    </GridPane>
</VBox>