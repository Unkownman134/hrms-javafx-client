<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox spacing="25.0" xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.gd.hrmsjavafxclient.controller.finance.SalaryManagementController"
      stylesheets="@../../style/hrms-styles.css"
      styleClass="root">
    <padding>
        <Insets top="30" right="30" bottom="30" left="30"/>
    </padding>

    <VBox spacing="15" styleClass="login-card" maxWidth="Infinity">
        <padding>
            <Insets top="25" right="30" bottom="25" left="30"/>
        </padding>
        <children>
            <Label text="💵 工资发放记录流水" style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: -fx-text-main;"/>
            <Separator />
            <HBox alignment="CENTER_LEFT" spacing="15">
                <children>
                    <Label text="筛选账期:" style="-fx-text-fill: -fx-text-muted; -fx-font-weight: bold;"/>
                    <ComboBox fx:id="monthFilterCombo" promptText="选择全部月份" prefWidth="200"
                              style="-fx-background-color: white; -fx-border-color: #D5DBDB; -fx-border-radius: 4;"/>

                    <Button text="重置条件" onAction="#handleReset"
                            style="-fx-background-color: #95A5A6; -fx-text-fill: white; -fx-cursor: hand; -fx-padding: 8 20;"/>

                    <Region HBox.hgrow="ALWAYS" />

                    <Button text="🔄 刷新数据" onAction="#loadData"
                            style="-fx-background-color: -fx-primary-color; -fx-text-fill: white; -fx-cursor: hand; -fx-padding: 8 20; -fx-font-weight: bold;"/>
                </children>
            </HBox>
        </children>
    </VBox>

    <VBox VBox.vgrow="ALWAYS" styleClass="login-card" maxWidth="Infinity">
        <padding>
            <Insets top="20" right="20" bottom="20" left="20"/>
        </padding>
        <children>
            <TableView fx:id="salaryTable" VBox.vgrow="ALWAYS" styleClass="data-table">
                <columns>
                    <TableColumn fx:id="colId" text="流水号" prefWidth="100"/>
                    <TableColumn fx:id="colEmpId" text="员工ID" prefWidth="100"/>
                    <TableColumn fx:id="colMonth" text="工资月份" prefWidth="180"/>
                    <TableColumn fx:id="colGross" text="应发总额 (￥)" prefWidth="150"/>
                    <TableColumn fx:id="colTax" text="个人所得税 (￥)" prefWidth="150"/>
                    <TableColumn fx:id="colNet" text="实发净额 (￥)" prefWidth="150"/>
                    <TableColumn fx:id="colDate" text="结算日期" prefWidth="180"/>
                </columns>
                <placeholder>
                    <Label text="暂无工资发放记录 Σ( ° △ °|||)" style="-fx-text-fill: -fx-text-muted;"/>
                </placeholder>
            </TableView>
        </children>
    </VBox>
</VBox>